#ifndef TYPES_H
#define TYPES_H

typedef enum {
  INSERT, REMOVE
} ActionType;

struct Action;
typedef struct Action Action;

struct Stack;
typedef struct Stack Stack;

struct History;
typedef struct History History;

#endif // !TYPES_H
